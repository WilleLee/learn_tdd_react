import { render, screen } from "@testing-library/react";
import App from "./App";

test("renders learn react link", () => {
  // render(<App />); // create a virtual DOM
  // const linkElement = screen.getByText(/learn testing library/i); // access the virtual DOM via screen
  // expect(linkElement).toBeInTheDocument(); // assert that the virtual DOM contains the text
  throw new Error("test");
});
